# Team Development Guide

How to set up development environment for multiple developers.

---

## 🎯 วิธีที่แนะนำ: Docker Compose (แนะนำที่สุด!)

### ข้อดี
- ✅ **แยกอิสระ** - แต่ละคน dev ในเครื่องตัวเอง ไม่กระทบกัน
- ✅ **ง่าย** - Clone repo มา รัน 2 คำสั่งเสร็จ
- ✅ **เหมือนกันทุกเครื่อง** - ไม่มีปัญหา "ในเครื่องผมทำงานนะ"
- ✅ **ไม่ต้องติดตั้งอะไร** - มีแค่ Docker พอ (ไม่ต้องติดตั้ง PostgreSQL, Node.js version ต่างๆ)
- ✅ **ปลอดภัย** - ทดสอบอะไรก็ได้ ไม่กระทบ production

### ข้อเสีย
- ⚠️ ต้องติดตั้ง Docker Desktop

### วิธีใช้

```bash
# 1. Clone repo
git clone <repo-url>
cd atsoko-backend

# 2. Copy environment
cp .env.docker .env

# 3. Start everything!
docker-compose up -d

# API ready at http://localhost:3000
```

อ่านเพิ่มเติม: [DOCKER-DEV.md](./DOCKER-DEV.md)

---

## 🔄 วิธีที่ 2: Local PostgreSQL (ตามที่คุณทำอยู่)

### ข้อดี
- ✅ No Docker needed
- ✅ Direct access to database

### ข้อเสีย
- ❌ **หลายคนแชร์ database เดียวกัน** - อาจทับข้อมูลกัน
- ❌ **แก้ schema ยาก** - คนหนึ่งแก้ คนอื่นต้องรัน migration ด้วย
- ❌ **Setup ยาก** - ต้องติดตั้ง PostgreSQL, import data ทุกเครื่อง
- ❌ **Version ไม่เหมือนกัน** - Node.js, PostgreSQL อาจเวอร์ชั่นต่างกัน

### วิธีใช้

```bash
# แต่ละคนต้องทำ:
1. ติดตั้ง PostgreSQL
2. ติดตั้ง Node.js 18
3. สร้าง database
4. Import schema
5. Import data (2,000+ records)
6. ตั้งค่า .env
```

### แนะนำให้ใช้เมื่อ
- ทีมเล็กมาก (1-2 คน)
- ไม่มี Docker
- ข้อมูลไม่ซับซ้อน

---

## 🌐 วิธีที่ 3: Shared Development Database (ไม่แนะนำ)

### ข้อดี
- ✅ ข้อมูลเหมือนกันทุกคน

### ข้อเสีย
- ❌ **อันตรายมาก** - คนหนึ่ง DROP TABLE ทุกคนได้รับผลกระทบ
- ❌ **ช้า** - ต้องเชื่อมต่อผ่าน network
- ❌ **ไม่มีความเป็นส่วนตัว** - ดูข้อมูลกันได้
- ❌ **Conflict** - หลายคนแก้พร้อมกัน อาจมีปัญหา

**ไม่แนะนำ!**

---

## 🚀 วิธีที่ 4: Backend บน VPS + Frontend Local

### ข้อดี
- ✅ Backend เดียวกันทุกคน

### ข้อเสีย
- ❌ **อันตรายมาก** - แก้โค้ดบน production โดยตรง
- ❌ **ทดสอบยาก** - กลัวพัง production
- ❌ **ช้า** - ทุก request ต้องผ่าน internet
- ❌ **ต้องมี internet ตลอด**

**ไม่แนะนำเลย!** ใช้ตอน production เท่านั้น

---

## 📊 สรุปเปรียบเทียบ

| วิธี | ความยาก | ความปลอดภัย | แนะนำ |
|------|---------|-------------|-------|
| **Docker Compose** | ⭐⭐ ง่าย | ✅ ปลอดภัยมาก | ✅ แนะนำที่สุด |
| **Local PostgreSQL** | ⭐⭐⭐ ยุ่งยาก | ⚠️ ปานกลาง | 🆗 ใช้ได้ |
| **Shared Database** | ⭐ ง่าย | ❌ อันตราย | ❌ ไม่แนะนำ |
| **VPS Backend** | ⭐ ง่าย | ❌ อันตรายมาก | ❌ ห้ามใช้! |

---

## 🎬 การทำงานกับทีม (Best Practice)

### Workflow แนะนำ

```
Developer A                     Developer B
    │                               │
    ├─ Clone repo                   ├─ Clone repo
    ├─ docker-compose up            ├─ docker-compose up
    │                               │
    ├─ Dev Feature A                ├─ Dev Feature B
    │  (Database A)                 │  (Database B)
    │                               │
    ├─ Test locally                 ├─ Test locally
    ├─ Commit & Push                ├─ Commit & Push
    │                               │
    └─────────┬─────────────────────┘
              │
              ▼
         Git Repository
              │
              ▼
      Deploy to VPS (Production)
```

### ขั้นตอน

1. **Development** - ใช้ Docker แต่ละคน
2. **Testing** - ทดสอบในเครื่องตัวเอง
3. **Code Review** - Pull Request บน Git
4. **Deployment** - Deploy ขึ้น VPS

---

## 💡 คำแนะนำ

### สำหรับคุณ (คนแรก)
```bash
# ใช้ Docker เลย!
docker-compose up -d
```

### สำหรับทีม Dev คนใหม่
```bash
# Clone มา
git clone <repo>
cd atsoko-backend

# Setup ใน 3 คำสั่ง
cp .env.docker .env
docker-compose up -d
docker-compose exec backend node scripts/import-data.js

# เสร็จ! เริ่ม dev ได้เลย
```

---

## ❓ คำถามที่พบบ่อย

### Q: ถ้าไม่อยากใช้ Docker ทำไง?
A: ใช้ Local PostgreSQL ได้ แต่แต่ละคนต้อง setup เอง และควรใช้ database ที่แยกกัน (ชื่อต่างกัน)

### Q: Production ใช้อะไร?
A: VPS ตามที่ตั้งค่าไว้ใน `/deploy` แล้ว (แยกจาก development)

### Q: ข้อมูล production เอามาใน Docker ได้ไหม?
A: ได้! Export จาก production แล้ว import ใน Docker

```bash
# Export from production
pg_dump -U atsoko_user thaiindustrialproperty_db > production.sql

# Import to Docker
docker-compose exec -T postgres psql -U postgres thaiindustrialproperty_db < production.sql
```

### Q: ทำไม Docker ดีกว่า?
A: เพราะแต่ละคน dev อิสระ ไม่กระทบกัน ไม่ต้องกลัวพัง และ setup ง่ายมาก!

---

**สรุป: ใช้ Docker Compose! ง่าย ปลอดภัย ทันสมัย** 🐳
