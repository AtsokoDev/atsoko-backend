# Rate Limiting Configuration for Nginx
# Include this in your nginx.conf or in http block

# Define rate limit zones
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=100r/m;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=300r/m;

# Define connection limits
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

# Usage example in server block:
# location /api/ {
#     limit_req zone=api_limit burst=20 nodelay;
#     limit_conn conn_limit 10;
#     ...
# }
