# Quick Start Guide สำหรับทีมใหม่

คู่มือเริ่มต้นอย่างรวดเร็วสำหรับ Developer ที่เข้ามาใหม่

> **📌 สำหรับผู้ใช้ Windows (CMD/PowerShell):**  
> อ่าน **[WINDOWS-SETUP.md](./WINDOWS-SETUP.md)** จะมีคำสั่งสำหรับ Windows โดยเฉพาะ

---

## 📋 สิ่งที่ต้องเตรียม

1. **Git** - สำหรับ clone โค้ด
2. **Docker Desktop** - สำหรับรัน development environment

---

## 🚀 ติดตั้งและเริ่มใช้งาน (5 ขั้นตอน)

### ขั้นตอนที่ 1: ติดตั้ง Docker

**ถ้ายังไม่มี Docker ให้อ่าน:** [DOCKER-INSTALL.md](./DOCKER-INSTALL.md)

**สรุปสั้นๆ:**
- **Windows:** ติดตั้ง [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)
- **macOS:** ติดตั้ง [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/)
- **Linux:** ติดตั้ง Docker Engine (ดูใน [DOCKER-INSTALL.md](./DOCKER-INSTALL.md))

**ตรวจสอบว่าติดตั้งสำเร็จ:**
```bash
docker --version
docker-compose --version
```

---

### ขั้นตอนที่ 2: Clone Repository

```bash
git clone <repository-url>
cd atsoko-backend
```

**ถ้าไม่มี repository URL:**
- ถาม Team Lead หรือผู้ดูแลโปรเจกต์
- หรือ copy โฟลเดอร์โปรเจกต์มาจากเพื่อนร่วมทีม

---

### ขั้นตอนที่ 3: Setup Environment

```bash
# Copy environment file
cp .env.docker .env
```

**ไฟล์นี้มีอะไร?**
- การตั้งค่า database
- Port ที่ใช้
- ไม่ต้องแก้อะไร ใช้ได้เลย!

---

### ขั้นตอนที่ 4: เริ่ม Docker Containers

```bash
# Start ทุกอย่าง (PostgreSQL + Backend)
docker-compose up -d
```

คำสั่งนี้จะ:
- ⬇️ Download Docker images (ครั้งแรกจะนานหน่อย)
- 🔨 Build application
- 🚀 Start containers

**รอประมาณ:** 2-3 นาทีครั้งแรก

---

### ขั้นตอนที่ 5: Import ข้อมูล

```bash
# Import properties data เข้า database
docker-compose exec backend node scripts/import-data.js
```

รอให้ import เสร็จ (ประมาณ 1-2 นาที)

---

## ✅ เสร็จแล้ว!

ตอนนี้คุณพร้อมใช้งานแล้ว! 🎉

**ทดสอบว่าทำงาน:**

1. เปิด browser ไปที่: http://localhost:3000
2. ควรจะเห็น: `{"message":"Backend API is running","version":"1.0.0","status":"OK"}`

**ลอง API อื่นๆ:**
- http://localhost:3000/api/properties
- http://localhost:3000/api/stats

---

## 💻 การใช้งานประจำวัน

### เริ่มทำงาน

```bash
# เปิด Docker Desktop (Windows/macOS)
# แล้วรัน:
cd atsoko-backend
docker-compose up -d
```

### ขณะ Coding

- แก้ไขโค้ดในโฟลเดอร์ `routes/`, `config/`, `server.js`
- บันทึกไฟล์
- **โค้ดจะ auto-reload!** (ไม่ต้อง restart)

### ดู Logs

```bash
# ดู logs แบบ real-time
docker-compose logs -f backend
```

กด `Ctrl+C` เพื่อหยุดดู logs

### หยุดทำงาน

```bash
# หยุด containers
docker-compose down
```

---

## 🔧 คำสั่งที่ใช้บ่อย

```bash
# เช็คสถานะ
docker-compose ps

# ดู logs
docker-compose logs -f backend

# Restart backend
docker-compose restart backend

# เข้า database
docker-compose exec postgres psql -U postgres -d thaiindustrialproperty_db

# Run คำสั่งใน backend container
docker-compose exec backend npm install <package-name>
```

---

## 🐛 แก้ปัญหาเบื้องต้น

### ปัญหา: Port 3000 ถูกใช้แล้ว

**แก้:**
1. เช็คว่ามีโปรแกรมอื่นใช้ port 3000 อยู่ไหม
2. ปิดโปรแกรมนั้น หรือ
3. เปลี่ยน port ใน `docker-compose.yml`:
   ```yaml
   ports:
     - "3001:3000"  # ใช้ 3001 แทน
   ```

### ปัญหา: Docker ไม่ทำงาน

**แก้:**
1. ตรวจสอบว่า Docker Desktop เปิดอยู่
2. Restart Docker Desktop
3. รัน `docker-compose up -d` ใหม่

### ปัญหา: ข้อมูลหาย

**แก้:**
```bash
# Import ข้อมูลใหม่
docker-compose exec backend node scripts/import-data.js
```

### ปัญหา: โค้ดไม่ auto-reload

**แก้:**
```bash
# Restart backend container
docker-compose restart backend
```

### ลบทุกอย่างและเริ่มใหม่

```bash
# ⚠️ คำเตือน: จะลบข้อมูลทั้งหมด
docker-compose down -v
docker-compose up -d --build
docker-compose exec backend node scripts/import-data.js
```

---

## 📚 เอกสารเพิ่มเติม

- **[DOCKER-INSTALL.md](./DOCKER-INSTALL.md)** - คู่มือติดตั้ง Docker
- **[DOCKER-DEV.md](./DOCKER-DEV.md)** - คู่มือ Docker แบบละเอียด
- **[TEAM-DEV.md](./TEAM-DEV.md)** - แนวทางการทำงานเป็นทีม
- **[README.md](./README.md)** - ข้อมูลโปรเจกต์

---

## 🆘 ต้องการความช่วยเหลือ?

1. ลอง Google ด้วย error message
2. ดูเอกสารข้างบน
3. ถามเพื่อนร่วมทีม
4. ถาม Team Lead

---

## ✅ Checklist สำหรับ Developer ใหม่

หลังจากตั้งค่าเสร็จแล้ว ให้เช็คว่า:

- [ ] Docker Desktop ติดตั้งแล้วและรันอยู่
- [ ] Clone repository แล้ว
- [ ] Copy `.env.docker` เป็น `.env` แล้ว
- [ ] รัน `docker-compose up -d` สำเร็จ
- [ ] Import data สำเร็จ (1,846 properties)
- [ ] เปิด http://localhost:3000 ได้
- [ ] แก้โค้ดแล้ว auto-reload
- [ ] ทดสอบ API ได้

ถ้าทุกข้อผ่าน แสดงว่าพร้อมเริ่มงานแล้ว! 🎊

---

**Happy Coding! 💻✨**
